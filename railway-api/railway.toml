[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"

# Daily scraping job - runs at 9:00 AM UTC every day
[[crons]]
schedule = "0 9 * * *"
command = "python generate_report.py"
